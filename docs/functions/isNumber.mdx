# $isNumber
Returns wheter the text inside is a number or not.

## Usage
> `$isNumber: [number]`

## Fields
| Name   | Description            | Type   | Level    |
| ------ | ---------------------- | ------ | -------- |
| number | The number to analyze. | Number | Required |

## Example
```yaml
$isNumber:[hi] # false
```